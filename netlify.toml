[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install && pnpm build"
  # Required for @netlify/plugin-nextjs (monorepo: .next lives under base)
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
